version: '2.4'
services:
  acclient:
    image: "maxipaolucci/atomic-coconut-client"
    mem_limit: 128m
    # this .env is generated by Elastic Beanstalk with its environment variables
    env_file:
      - .env
  acserver:
    image: "maxipaolucci/atomic-coconut-server"
    volumes:
      # this volume is to access the db backup files
      - "/home/ec2-user/db-bkp:/app/data/dev:rw"
    mem_limit: 128m
    # this .env is generated by Elastic Beanstalk with its environment variables
    env_file:
      - .env
    ports: 
      # xray daemon
      - "2000:2000"
    volumes:
      - "/var/elasticbeanstalk/xray:/var/elasticbeanstalk/xray"
  nginx:
    image: "maxipaolucci/atomic-coconut-nginx"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "/home/ec2-user/conf:/etc/letsencrypt:ro"
      - "/home/ec2-user/www:/var/www/certbot:ro"
    mem_limit: 128m
    # this .env is generated by Elastic Beanstalk with its environment variables
    env_file:
      - .env
    links:
      - acclient
      - acserver
  xraydaemon:
    image: "amazon/aws-xray-daemon"
    ports:
      - "2000/udp:2000/udp"
      - "2000/tcp:2000/tcp"
    mem_limit: 256m
    